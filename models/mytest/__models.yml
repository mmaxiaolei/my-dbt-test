version: 2

models:
  - name: model1
    description: just a test model
    columns:
      - name: id
        description: The unique key
        tests:
          - not_null
          - unique
      - name: name
        description: full name.